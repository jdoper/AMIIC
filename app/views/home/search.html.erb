<div class="container">
	<h2 class="name-header">Busca</h2>

	<%= form_tag('/buscar', remote: true) %>
		<div class="form-group">
			<%= number_field_tag :registro, nil, class: 'form-control' %>
		</div>
		<div class="form-group">
			<%= submit_tag "Buscar", :class => 'btn btn-success' %>
		</div>
	
	<br>

	<h3 class="name-header">Cadastros encontrados</h3>

	<div id="filter">
	  <%= render 'list' %>
	</div>
</div>